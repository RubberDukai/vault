# Nothing under library/ is ever tracked: packs, charts, books and any partial download
library/

# Runtime state: profiles, review schedules, lesson progress
data/

# Scratch
.tmp/
node_modules/
*.log

# Built installers. Big binaries do not belong in git; build them with
# node tools/make-installer.js when they are needed.
dist/
